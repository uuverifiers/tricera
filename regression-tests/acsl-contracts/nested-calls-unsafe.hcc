/*@ requires x >= 0; ensures \result == x + 1; */
int inc(int x) {
  return x + 1;
}

/*@ ensures \result == x + y; */
int add(int x, int y) {
  return x + y;
}

int main() {
  add(inc(inc(0)), inc(inc(0) - 2));
  return 0;
}
