int main(void) {
  int x = 1;
  //@ assert \let x = x + 1; (\let x = x + 1; x == 4) && x == 2;
  //@ assert x == 1;
  //@ assert (\let y = x + 1; y + 1) == 3;
  //@ assert \let p = x == 1; p;
  //@ assert \forall integer y; \let x = y; \exists integer y; y > x;
  return 0;
}
