
get-1.c
Warning: no definition of function "non_det_int" available
Warning: no definition of function "non_det_int_ptr" available

Inferred ACSL annotations
================================================================================
/* contract for get */
/*@
  requires n == p && n_init >= 1 && \valid(p);
  ensures n == \old(p) && \old(n_init) == n_init && \old(r1) == r1 && \result >= 0 && (n_init == \result || \old(*p) != n_init) && \valid(p);
*/
================================================================================

SAFE

incdec-1.c
Warning: no definition of function "non_det_int" available
Warning: no definition of function "non_det_int_ptr" available

Inferred ACSL annotations
================================================================================
/* contract for decrement */
/*@
  requires val == b && \valid(a) && \valid(b) && \separated(a, b);
  ensures \old(b) == b && \old(a) == a && \old(b_init) == b_init && \old(a_init) == a_init && \valid(val) && \valid(b) && \valid(a) && \separated(a, val) && \separated(a, b) && \old(*val) - *val == 1;
  assigns *val;
*/
/* contract for increment */
/*@
  requires a == val && \valid(b) && \valid(val) && \separated(val, b);
  ensures \old(b) == b && \old(b_init) == b_init && \old(a_init) == a_init && a == \old(val) && \valid(b) && \valid(val) && \separated(val, b) && \old(*val) - *val == -1;
  assigns *val;
*/
================================================================================

SAFE

incdec-2.c
Warning: no definition of function "non_det_int" available
Warning: no definition of function "non_det_int_ptr" available

Inferred ACSL annotations
================================================================================
/* contract for decrement */
/*@
  requires val == a && \valid(a);
  ensures \old(a) == a && \old(a_init) == a_init && \valid(val) && \valid(a) && \old(*val) - *val == 1;
  assigns *val;
*/
/* contract for increment */
/*@
  requires a == val && \valid(val);
  ensures \old(a_init) == a_init && a == \old(val) && \valid(val) && \old(*val) - *val == -1;
  assigns *val;
*/
================================================================================

SAFE

max-1.c
Warning: no definition of function "non_det_int" available
Warning: no definition of function "non_det_int_ptr" available

Inferred ACSL annotations
================================================================================
/* contract for findMax */
/*@
  requires b == y && a == x && \valid(y) && \valid(x) && *y == b_init && *x == a_init;
  ensures b == \old(y) && a == \old(x) && \old(b_init) == b_init && \old(a_init) == a_init && \old(r) == r && \valid(y) && \valid(x) && ((\result == b_init && b_init - a_init >= 1) || (\result == a_init && a_init >= b_init));
  assigns \nothing;
*/
================================================================================

SAFE

max-2.c
Warning: no definition of function "non_det_int_ptr" available
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for findMax */
/*@
  requires b == y && a == x && r == max && \valid(y) && \valid(x) && \valid(max) && *y == b_init && *x == a_init;
  ensures b_init == \old(b_init) && a_init == \old(a_init) && \old(y) == b && \old(x) == a && \old(b) == b && \old(a) == a && r == \old(max) && \valid(max) && (\old(b_init) - \old(a_init) >= 1 || (\old(a_init) >= \old(b_init) && *max == \old(a_init))) && (*max == \old(b_init) || (\old(a_init) >= \old(b_init) && *max == \old(a_init))) && \valid(x) && \valid(y);
*/
================================================================================

SAFE

multadd-1.c
Warning: no definition of function "non_det_int_ptr" available
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for addTwoNumbers */
/*@
  requires \valid(result) && \valid(b) && \valid(a);
  ensures \old(b_init) == b_init && \old(a_init) == a_init && \valid(result) && \valid(b) && \valid(a) && \old(*b) + \old(*a) - *result == 0;
*/
/* contract for multiplyByTwo */
/*@
  requires a == num && \valid(result) && \valid(b) && \valid(num) && \separated(b, result) && \separated(num, result) && \separated(num, b);
  ensures \old(b_init) == b_init && \old(a_init) == a_init && \old(result) == result && \old(b) == b && a == \old(num) && \valid(result) && \valid(b) && \valid(num) && \separated(b, result) && \separated(num, result) && \separated(num, b) && 2*\old(*num) == *num;
  assigns *num;
*/
================================================================================

SAFE

truck-2.c

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Init:
                                                                                        main21_1(heap.empty, 0, 0, 0, heap.empty, 0, 0, 0)                                                                                        
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
                                                                                                                |                                                                                                                 
                                                                                                                |                                                                                                                 
                                                                                                                V                                                                                                                 
 main46_22(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.empty, O_Truck(Truck(5, heap.addr(3))))), O_Human(Human(2)))), O_Human(Human(3)))), 2, 3, 5, heap.empty, 0, 0, 0, 1) 
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Final:
 main46_22(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.empty, O_Truck(Truck(5, heap.addr(3))))), O_Human(Human(2)))), O_Human(Human(3)))), 2, 3, 5, heap.empty, 0, 0, 0, 1) 
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Failed assertion:

UNSAFE

heapptr-to-stackptr.c

Inferred ACSL annotations
================================================================================
/* contract for swap */
/*@
  requires \valid(y) && \valid(x);
  ensures \valid(y) && \valid(x) && *y == \old(*x) && \old(*y) == *x;
  assigns *x, *y;
*/
================================================================================

SAFE

qualified-heapptr-to-stackptr.c

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Init:
                                                                             main11_3(heap.empty, heap.null, heap.null)                                                                              
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
                                                                                                  |                                                                                                  
                                                                                                  |                                                                                                  
                                                                                                  V                                                                                                  
 main14_3_10(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.empty, O_int(47))), O_int(42))), heap.addr(1), heap.addr(2), heap.addr(1), heap.addr(2), 3, 4, heap.null, heap.null) 
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
                                                                                                  |                                                                                                  
                                                                                                  |                                                                                                  
                                                                                                  V                                                                                                  
                    main_2(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.empty, O_int(47))), O_int(42))), heap.null, heap.null, heap.addr(2), heap.addr(1))                     
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Final:
                    main_2(heap.heapAddrPair_1(heap.alloc(heap.heapAddrPair_1(heap.alloc(heap.empty, O_int(47))), O_int(42))), heap.null, heap.null, heap.addr(2), heap.addr(1))                     
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Failed assertion:
false :- main_2(@h, globalswap_0_1x:14, globalswap_0_1y:14, a:11, b:12), get_int(heap.read(@h, a:11)) != 42 | get_int(heap.read(@h, b:12)) != 0. (line:15 col:3) (property: user-assertion)

UNSAFE

multi-arg-1.c

Inferred ACSL annotations
================================================================================
/* contract for mod */
/*@
  requires \valid(t1) && \valid(t2) && \separated(t1, t2);
  ensures \valid(t2) && \valid(t1) && \old(*t1) - *t1 == -1 && \old(*t2) - *t2 == 1;
  assigns *t1, *t2;
*/
================================================================================

SAFE

multi-arg-2.c

Inferred ACSL annotations
================================================================================
/* contract for mod */
/*@
  requires \valid(t4) && \valid(t3) && \valid(t2) && \valid(t1) && \separated(t3, t4) && \separated(t2, t4) && \separated(t2, t3) && \separated(t1, t4) && \separated(t1, t3) && \separated(t1, t2);
  ensures \valid(t4) && \valid(t3) && \valid(t2) && \valid(t1) && \separated(t3, t4) && \separated(t2, t4) && \separated(t2, t3) && \separated(t1, t4) && \separated(t1, t3) && \separated(t1, t2) && 2*\old(*t3) == *t3 && \old(*t2) - *t2 == 1 && *t1 - \old(*t1) == 2 && 2*\old(*t1) - *t4 == -4;
  assigns *t1, *t3, *t2, *t4;
*/
================================================================================

SAFE

alias-share-safe.c

Inferred ACSL annotations
================================================================================
/* contract for f */
/*@
  requires p == q && \valid(q);
  ensures \result == 2 && \valid(p) && \valid(q) && *q == 2;
  assigns *p, *q;
*/
================================================================================

SAFE

alias-share-unsafe.c

--------------------------------------
Init:
      main9_5(heap.empty, 0, 0)       
--------------------------------------
                  |                   
                  |                   
                  V                   
 main9_5_8(heap.empty, 0, 0, 2, 2, 0) 
--------------------------------------
                  |                   
                  |                   
                  V                   
        main(heap.empty, 0, 2)        
--------------------------------------
Final:
        main(heap.empty, 0, 2)        
--------------------------------------
Failed assertion:
false :- main(@h, globalf_0_0p:9, x:8), x:8 != 1. (line:11 col:5) (property: user-assertion)

UNSAFE

alias-mixed-calls.c

Inferred ACSL annotations
================================================================================
/* contract for twice */
/*@
  requires \true;
  ensures \result == 2*\old(n);
  assigns \nothing;
*/
/* contract for update */
/*@
  requires \valid(p) && \valid(q) && ((p == q && step == 3) || (step == 4 && \separated(p, q) && *p == 2 && *q == 5)) && (*q == 2 || (\separated(p, q) && *p == 2 && *q == 5));
  ensures (5 - \old(*q) >= 1 || (\result == 3 && \valid(p) && \valid(q) && *p == 3 && *q == 9)) && (\old(q) != \old(p) || (\result == 6 && \valid(p) && \valid(q) && *q == 6)) && \valid(p) && \valid(q);
*/
================================================================================

SAFE

alias-reject-overlap.c
UNKNOWN (Unsupported C fragment. Cannot decide whether the stack-pointer arguments to contract function 'f' may alias. Pass the same lvalue, or distinct simple variables/fields, to its pointer parameters.)

alias-reject-array.c
UNKNOWN (Unsupported C fragment. Array-pointer conversion for argument 2 of function f is not supported.)

alias-reject-deref.c
UNKNOWN (Unsupported C fragment. Cannot decide whether the stack-pointer arguments to contract function 'f' may alias. Pass the same lvalue, or distinct simple variables/fields, to its pointer parameters.)

max-2.c (array)
Warning: no definition of function "non_det_int_ptr" available
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for findMax */
/*@
  requires b == y && a == x && r == max && \valid(y) && \valid(x) && \valid(max) && *y == b_init && *x == a_init;
  ensures b_init == \old(b_init) && a_init == \old(a_init) && \old(y) == b && \old(x) == a && \old(b) == b && \old(a) == a && r == \old(max) && \valid(max) && (\old(a_init) >= \old(b_init) || (\old(b_init) - \old(a_init) >= 1 && *max == \old(b_init))) && (*max == \old(a_init) || (\old(b_init) - \old(a_init) >= 1 && *max == \old(b_init))) && \valid(x) && \valid(y);
*/
================================================================================

SAFE

frame-globals-only.c

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires \true;
  ensures count == 3 + \old(count) && unchanged == \old(unchanged);
  assigns count;
*/
/* contract for read_count */
/*@
  requires \true;
  ensures count == \old(count) && unchanged == \old(unchanged) && \result == \old(count);
  assigns \nothing;
*/
================================================================================

SAFE

strengthen-globals.c

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires required == 5;
  ensures required == 5 && counter == 1 + \old(counter) && unchanged == \old(unchanged) && \result == 1 + \old(n);
  assigns counter;
*/
================================================================================

SAFE

strengthen-conditional.c

Inferred ACSL annotations
================================================================================
/* contract for at_one */
/*@
  requires n == 1;
  ensures \old(n) == 1 && \result == 2;
  assigns \nothing;
*/
================================================================================

SAFE

strengthen-guards.c
Warning: no definition of function "nondet" available

Inferred ACSL annotations
================================================================================
/* contract for at_ten */
/*@
  requires \true;
  ensures 1 >= \result && \result >= 0 && (\old(x) != 10 || \result >= 1);
  assigns \nothing;
*/
/* contract for positive */
/*@
  requires \true;
  ensures 1 >= \result && \result >= 0 && (0 >= \old(x) || \result >= 1);
  assigns \nothing;
*/
================================================================================

SAFE

strengthen-assertion.c

Inferred ACSL annotations
================================================================================
/* contract for positive */
/*@
  requires n == 1;
  ensures \old(n) == 1 && \result == 1;
  assigns \nothing;
*/
================================================================================

SAFE

call-site-frame.c (native)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires \valid(p);
  ensures \valid(p) && \old(*p) - *p == -1;
  assigns *p;
*/
================================================================================

SAFE

frame-globals.c (native)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires \valid(p);
  ensures count == 1 + \old(count) && \valid(p) && \old(*p) - *p == -1;
  assigns count, *p;
*/
================================================================================

SAFE

frame-allocate.c (native)

Inferred ACSL annotations
================================================================================
/* contract for allocate */
/*@
  requires \true;
  ensures \valid(p) && *p == 7;
*/
================================================================================

SAFE

alias-heap-stack.c (native)

Inferred ACSL annotations
================================================================================
/* contract for bump */
/*@
  requires \valid(r);
  ensures \valid(r) && *r == 1 + \old(*r);
  assigns *r;
*/
/* contract for update */
/*@
  requires \valid(q) && \valid(p) && *p == 2 && *q >= 5;
  ensures \result == 3 && \valid(q) && \valid(p) && \old(*q) - *q == 1 && *p == 3;
  assigns *p, *q;
*/
================================================================================

SAFE

read-free.c (native)

Inferred ACSL annotations
================================================================================
/* contract for release */
/*@
  requires \valid(p);
  ensures \old(*p) == \result;
*/
================================================================================

SAFE

free-other.c (native)

Inferred ACSL annotations
================================================================================
/* contract for release */
/*@
  requires \valid(q) && \valid(p) && \separated(p, q);
  ensures \valid(p);
*/
================================================================================

SAFE

conditional-free.c (native)

Inferred ACSL annotations
================================================================================
/* contract for release */
/*@
  requires \valid(p);
  ensures \result == \old(*p);
*/
================================================================================

SAFE

global-pointer-reset.c (native)

Inferred ACSL annotations
================================================================================
/* contract for reset */
/*@
  requires \valid(g);
  ensures \valid(\old(g)) && *\old(g) == \result;
  assigns g;
*/
================================================================================

SAFE

call-site-frame.c (array)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires \valid(p);
  ensures \valid(p) && \old(*p) - *p == -1;
  assigns *p;
*/
================================================================================

SAFE

frame-globals.c (array)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires \valid(p);
  ensures count == 1 + \old(count) && \valid(p) && \old(*p) - *p == -1;
  assigns count, *p;
*/
================================================================================

SAFE

frame-allocate.c (array)

Inferred ACSL annotations
================================================================================
/* contract for allocate */
/*@
  requires \true;
  ensures \valid(p) && *p == 7;
*/
================================================================================

SAFE

alias-heap-stack.c (array)

Inferred ACSL annotations
================================================================================
/* contract for bump */
/*@
  requires \valid(r);
  ensures \valid(r) && *r == 1 + \old(*r);
  assigns *r;
*/
/* contract for update */
/*@
  requires \valid(q) && \valid(p) && *p == 2 && *q >= 5;
  ensures \result == 3 && \valid(q) && \valid(p) && \old(*q) - *q == 1 && *p == 3;
  assigns *p, *q;
*/
================================================================================

SAFE

read-free.c (array)

Inferred ACSL annotations
================================================================================
/* contract for release */
/*@
  requires \valid(p);
  ensures \old(*p) == \result;
*/
================================================================================

SAFE

free-other.c (array)

Inferred ACSL annotations
================================================================================
/* contract for release */
/*@
  requires \valid(q) && \valid(p) && \separated(p, q);
  ensures \valid(p);
*/
================================================================================

SAFE

conditional-free.c (array)

Inferred ACSL annotations
================================================================================
/* contract for release */
/*@
  requires \valid(p);
  ensures \result == \old(*p);
*/
================================================================================

SAFE

global-pointer-reset.c (array)

Inferred ACSL annotations
================================================================================
/* contract for reset */
/*@
  requires \valid(g);
  ensures \valid(\old(g)) && *\old(g) == \result;
  assigns g;
*/
================================================================================

SAFE

struct-field-read.c

Inferred ACSL annotations
================================================================================
/* contract for f_with_arg */
/*@
  requires \valid(s);
  ensures s->x - \result == -1 && \valid(s);
  assigns \nothing;
*/
================================================================================

SAFE

enum-indexed-struct.c

Inferred ACSL annotations
================================================================================
/* contract for helper_read_record */
/*@
  requires slot >= 0 && 2 - slot >= 1;
  ensures records == \old(records) && \old(records)[slot] == \result && \valid(records);
  assigns \nothing;
*/
================================================================================

SAFE

enum-indexed-struct-field.c

Inferred ACSL annotations
================================================================================
/* contract for helper_read_level */
/*@
  requires slot >= 0 && 2 - slot >= 1;
  ensures records == \old(records) && \old(records)[slot].level == \result && \valid(records);
  assigns \nothing;
*/
================================================================================

SAFE

global-array-increment-safe.c (native)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires x == a && 3 - n >= 1;
  ensures a == \old(x) && \old(x[n]) - x[n] == -1 && \valid(x);
  assigns x[n];
*/
================================================================================

SAFE

global-array-offset.c (native)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires n == 1 && (a + 1) == x;
  ensures \old(a) == a && \old(a[2]) - \old(a)[2] == -1 && \valid(x) && \valid(a);
  assigns a[2];
*/
================================================================================

SAFE

global-array-call-aliases.c (native)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires (b == x && 5 - n >= 1) || (x == a && 3 - n >= 1);
  ensures \old(b) == b && \old(a) == a && \old(x[n]) - x[n] == -1 && \valid(x) && \valid(a) && \valid(b);
  assigns x[n];
*/
================================================================================

SAFE

global-array-increment-unsafe.c (native)
UNSAFE

global-array-pointer-alias-unsafe.c (native)
UNSAFE

global-array-pointer-reassign-safe.c (native)

Inferred ACSL annotations
================================================================================
/* contract for reassign */
/*@
  requires p == a;
  ensures \old(p) == a && \old(b) == b && \old(a) == a && p == b && \valid(a) && \valid(b);
*/
================================================================================

SAFE

global-array-offset.c (native, initialized)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires (a + 1) == x && 4 - (1 + n) >= 1;
  ensures a == \old(a) && x[n] - \old(x[n]) == 1 && \valid(x) && \valid(a);
*/
================================================================================

SAFE

global-array-increment-safe.c (array)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires x == a && 3 - n >= 1;
  ensures a == \old(x) && \old(x[n]) - x[n] == -1 && \valid(x);
  assigns x[n];
*/
================================================================================

SAFE

global-array-offset.c (array)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires n == 1 && (a + 1) == x;
  ensures \old(a) == a && \old(a[2]) - \old(a)[2] == -1 && \valid(x) && \valid(a);
  assigns a[2];
*/
================================================================================

SAFE

global-array-call-aliases.c (array)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires (b == x && 5 - n >= 1) || (x == a && 3 - n >= 1);
  ensures \old(b) == b && \old(a) == a && \old(x[n]) - x[n] == -1;
  assigns x[n];
*/
================================================================================

SAFE

global-array-increment-unsafe.c (array)
UNSAFE

global-array-pointer-alias-unsafe.c (array)
UNSAFE

global-array-pointer-reassign-safe.c (array)

Inferred ACSL annotations
================================================================================
/* contract for reassign */
/*@
  requires p == a;
  ensures \old(p) == a && \old(b) == b && \old(a) == a && p == b && \valid(a) && \valid(b);
*/
================================================================================

SAFE

global-array-offset.c (array, initialized)

Inferred ACSL annotations
================================================================================
/* contract for increment */
/*@
  requires (a + 1) == x && 4 - (1 + n) >= 1;
  ensures a == \old(a) && \old(a)[(1 + n)] == 1 + \old(a[(1 + n)]) && \valid(x) && \valid(a);
*/
================================================================================

SAFE
