// the contract is contradictory at the call, the code after it is unreachable
int g = 0;
/*@ assigns g;
    ensures g == 1 && g == 2; @*/
extern void f(void);
int main(void) {
  f();
  /*@ assert g == 1; @*/
  return 0;
}
