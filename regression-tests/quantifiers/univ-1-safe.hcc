void main() {
  int n = _;
  assume(n > 0);
  int a[];
  int i = 0;
  for(; i < n; ++i) 
  {
    a[i] = i-1;
  }

  //@ assert \forall int j; ((0 <= j) && (j <= n-1)) ==> (a[j] == j-1);

  /* This assertion is not in the exact format we recognize,
     so extended quantifiers are not used to encode it.
  */
}
