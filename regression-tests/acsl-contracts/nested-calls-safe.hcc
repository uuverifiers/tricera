/*@ requires x >= 0; ensures \result == x + 1; */
int inc(int x) {
  return x + 1;
}

/*@ ensures \result == x + y; */
int add(int x, int y) {
  return x + y;
}

int main() {
  assert(add(inc(0), inc(1)) == 3);
  assert(add(inc(inc(0)), inc(inc(1))) == 5);
  return 0;
}
