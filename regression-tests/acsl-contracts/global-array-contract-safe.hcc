// SAFE: an array global referenced only from a contract must not be sliced by
// tri-pp (decl-slicing is disabled when ACSL annotations are present).

int g[5];

/*@ ensures g[0] == \old(g[0]); */
void f(void) {
}

int main(void) {
  f();
  return 0;
}
