
contract1.hcc

Inferred ACSL annotations
================================================================================
/* contract for f */
/*@
  requires \true;
  ensures \result >= 0 && (0 >= \old(x) || \result >= 1);
  assigns \nothing;
*/
================================================================================

SAFE

contract2.hcc

Inferred ACSL annotations
================================================================================
/* contract for f */
/*@
  requires g >= 0;
  ensures g >= 1 && \result >= 0 && \result >= \old(x);
  assigns g;
*/
================================================================================

SAFE

contract2b.hcc

---------------------
Init:
  main15_3(-10, 5)   
---------------------
          |          
          |          
          V          
 main16_7(-10, 5, 5) 
---------------------
          |          
          |          
          V          
    main_1(-4, 5)    
---------------------
          |          
          |          
          V          
    main_2(-4, 5)    
---------------------
Final:
    main_2(-4, 5)    
---------------------
Failed assertion:
false :- main_2(g:2, y:14), g:2 < 0. (line:18 col:3) (property: user-assertion)

UNSAFE

contract3.hcc

Inferred ACSL annotations
================================================================================
/* contract for f */
/*@
  requires g >= 0;
  ensures g >= 0 && g - \old(g) >= \old(x);
  assigns g;
*/
================================================================================

SAFE

takeuchi.hcc

Inferred ACSL annotations
================================================================================
/* contract for tak */
/*@
  requires \true;
  ensures (\old(y) == \result || \old(z) == \result || \old(y) - \old(z) >= 1) && (\old(y) == \result || \old(x) - \old(y) >= 1) && (\old(y) >= \old(z) || ((\old(y) != \result || \result >= \old(x)) && (\old(z) != \result || \old(x) - \old(y) >= 1)));
  assigns \nothing;
*/
================================================================================

SAFE

assert.hcc

Inferred ACSL annotations
================================================================================
/* contract for mult */
/*@
  requires N == x && x >= 0;
  ensures \old(x) == N && \old(N) == N && \old(N2) == N2 && \result >= 0 && N >= 0 && (\result >= \old(y) || 0 >= N);
  assigns \nothing;
*/
================================================================================

SAFE

fib.hcc

---------------
Init:
   main12_3    
---------------
       |       
       |       
       V       
 main12_11(6)  
---------------
       |       
       |       
       V       
 main12_3_1(8) 
---------------
Final:
 main12_3_1(8) 
---------------
Failed assertion:
false :- main12_3_1(x:12), x:12 != 0. (line:14 col:3) (property: user-assertion)

UNSAFE

stackptr.hcc

Inferred ACSL annotations
================================================================================
/* contract for f */
/*@
  requires \valid(x);
  ensures \valid(x) && \old(*x) - *x == -1;
  assigns *x;
*/
================================================================================

SAFE

struct-name-bug.hcc

Inferred ACSL annotations
================================================================================
/* contract for foo */
/*@
  requires \true;
  ensures \result == 2*\old(s).f;
  assigns \nothing;
*/
================================================================================

SAFE

stackptr-func-dec.hcc

Inferred ACSL annotations
================================================================================
/* contract for f */
/*@
  requires \valid(x);
  ensures \valid(x) && \old(*x) - *x == -1;
  assigns *x;
*/
================================================================================

SAFE

stackptr-multi-arg.hcc
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for mod */
/*@
  requires \valid(t1) && \valid(t2) && \separated(t1, t2);
  ensures \valid(t1) && \valid(t2) && \old(t1->val) - t1->val == -1 && \old(t2->val) - t2->val == 1;
  assigns *t1, *t2;
*/
================================================================================

SAFE

stackptr-nested-calls.hcc
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for decr */
/*@
  requires \valid(q);
  ensures \valid(q) && \old(q->val) - q->val == 1;
  assigns *q;
*/
/* contract for incdec */
/*@
  requires \valid(p) && 2147483646 >= p->val && p->val >= -2147483648;
  ensures \valid(p) && \old(p->val) == p->val && 2147483646 >= p->val && p->val >= -2147483648;
  assigns *p;
*/
================================================================================

SAFE

stackptr-recursion.hcc

------------------------------------
Init:
     main25_5(heap.empty, 0, 0)     
------------------------------------
                 |                  
                 |                  
                 V                  
 main25_5_7(heap.empty, 0, 0, 2, 0) 
------------------------------------
                 |                  
                 |                  
                 V                  
       main(heap.empty, 0, 1)       
------------------------------------
Final:
       main(heap.empty, 0, 1)       
------------------------------------
Failed assertion:
false :- main(@h, globalincr_0t:25, s:24), s:24 != 5. (line:26 col:5) (property: user-assertion)

UNSAFE

stackptr-retvalue.hcc
UNKNOWN (Unsupported C fragment. 8:12 Stack pointers in combination with heap pointers)

stackptr-struct.hcc
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for incr */
/*@
  requires \valid(t);
  ensures \valid(t) && \old(t->val) - t->val == -1;
  assigns *t;
*/
================================================================================

SAFE

stackptr-custom-entry.hcc
Warning: no definition of function "non_det_int" available

Inferred ACSL annotations
================================================================================
/* contract for incr */
/*@
  requires \valid(t);
  ensures \valid(t) && \old(t->val) - t->val == -1;
  assigns *t;
*/
================================================================================

SAFE

stackptr-ilp32.hcc
Warning: The following clause has different terms with the same name (term: globalf1_0p:10)
main10_5(@h, globalf1_0p:10, c:9.\as[signed bv[32]]) :- c:9 >= -2147483648 & 2147483647 >= c:9 & globalf1_0p:10 >= -2147483648 & 2147483647 >= globalf1_0p:10 & @h = heap.empty & globalf1_0p:10 = 0.\as[signed bv[32]].


Inferred ACSL annotations
================================================================================
/* contract for f1 */
/*@
  requires \valid(p);
  ensures \valid(p) && *p == 0;
  assigns *p;
*/
================================================================================

SAFE
