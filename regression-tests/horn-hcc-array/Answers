
out-of-bounds-line1.c
Warning: enabling heap model
Warning: no definition of function "nondet" available
UNSAFE

-----------------------------------------------------------------------------
Init:
                main2(emptyHeap, AddrRangeADT(nthAddr(0), 0))                
-----------------------------------------------------------------------------
                                      |                                      
                                      |                                      
                                      V                                      
 main6(newHeap(alloc(emptyHeap, O_Int(11))), AddrRangeADT(nthAddr(1), 1), 1) 
-----------------------------------------------------------------------------
Final:
 main6(newHeap(alloc(emptyHeap, O_Int(11))), AddrRangeADT(nthAddr(1), 1), 1) 
-----------------------------------------------------------------------------
valid-memsafety
  expected verdict matches the result!

out-of-bounds-line2.c
Warning: enabling heap model
UNSAFE

----------------------------------------------------------------------------------------
Init:
 main2(newBatchHeap(batchAlloc(emptyHeap, O_Int(0), 42)), AddrRangeADT(nthAddr(1), 42)) 
----------------------------------------------------------------------------------------
Final:
 main2(newBatchHeap(batchAlloc(emptyHeap, O_Int(0), 42)), AddrRangeADT(nthAddr(1), 42)) 
----------------------------------------------------------------------------------------
valid-memsafety
  expected verdict matches the result!

out-of-bounds-line3.c
Warning: enabling heap model
Warning: no definition of function "nondet" available
UNSAFE

-----------------------------------------------------------------------------
Init:
                main2(emptyHeap, AddrRangeADT(nthAddr(0), 0))                
-----------------------------------------------------------------------------
                                      |                                      
                                      |                                      
                                      V                                      
 main6(newHeap(alloc(emptyHeap, O_Int(11))), AddrRangeADT(nthAddr(1), 1), 1) 
-----------------------------------------------------------------------------
Final:
 main6(newHeap(alloc(emptyHeap, O_Int(11))), AddrRangeADT(nthAddr(1), 1), 1) 
-----------------------------------------------------------------------------
valid-memsafety
  expected verdict matches the result!

out-of-bounds-line4.c
Warning: enabling heap model
UNSAFE

----------------------------------------------------------------------------------------
Init:
 main2(newBatchHeap(batchAlloc(emptyHeap, O_Int(0), 42)), AddrRangeADT(nthAddr(1), 42)) 
----------------------------------------------------------------------------------------
Final:
 main2(newBatchHeap(batchAlloc(emptyHeap, O_Int(0), 42)), AddrRangeADT(nthAddr(1), 42)) 
----------------------------------------------------------------------------------------
valid-memsafety
  expected verdict matches the result!

dynamic-loop1.c
Warning: enabling heap model
SAFE
valid-memsafety
  expected verdict matches the result!
unreach-call
  expected verdict matches the result!

simple-dynamic-array.c
Warning: enabling heap model
SAFE
valid-memsafety
  expected verdict matches the result!
unreach-call
  expected verdict matches the result!

simple-global-array.c
Warning: enabling heap model
SAFE
valid-memsafety
  expected verdict matches the result!
unreach-call
  expected verdict matches the result!
